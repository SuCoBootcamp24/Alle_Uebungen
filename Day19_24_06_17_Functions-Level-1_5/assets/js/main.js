//1.

function math(num, anotherNum) {
  console.log(
    `Das Ergebnis der Multiplikation von ${num} * ${anotherNum} ist : ${
      num * anotherNum
    }.`
  );

  console.log(
    `Das Ergebnis der Division von ${num} / ${anotherNum} ist : ${
      num / anotherNum
    }.`
  );
}
